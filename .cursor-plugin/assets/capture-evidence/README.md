# Demo-flow evidence (validation metadata)

| Artifact | Purpose |
| --- | --- |
| `demo-flow-evidence.json` | Deterministic MCP CLI outputs recorded for reviewers (`scripts/record_cursor_marketplace_demo_evidence.py`) |

This is **not** a substitute for real Cursor UI listing screenshots or hero banners.

Regenerate:

```bash
python3 scripts/record_cursor_marketplace_demo_evidence.py
```
