# Screenshot plan (Cursor Marketplace)

Capture **1920×1080** stills with dark theme, no personal email notifications visible.

1. **MCP connected** — GovAI server entry showing green status and tool list header.
2. **`govai_gate_reports` success** — output panel showing sorted JSON keys and `"ok": true`.
3. **Evidence pack validation** — path to **`governance_evidence_pack.valid.json`** visible; success line present.
4. **Audit template dry-run** — response includes `wrote_file: false` (or equivalent) and preview body.
5. **Rules panel** — `.mdc` titles visible: branch policy, audit report required, compliance gate.

## Alt text guidance

Each image should describe the **governance action** being shown, not Cursor chrome jargon.

## Motion optional

If video is requested, record at most **720p**, **60 seconds**, voice-over reading the demo flow script.
