apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aigov-core.fullname" . }}
  labels:
    {{- include "aigov-core.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
