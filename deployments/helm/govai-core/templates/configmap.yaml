apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "govai-core.fullname" . }}
  labels:
    {{- include "govai-core.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
