apiVersion: v1
kind: Service
metadata:
  name: {{ include "govai-core.fullname" . }}
  labels:
    {{- include "govai-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "govai-core.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
