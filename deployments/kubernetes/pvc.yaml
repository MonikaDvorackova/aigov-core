apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: govai-ledger
  namespace: aigov-core
  labels:
    app.kubernetes.io/name: aigov-core
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # storageClassName: standard  # set per cluster
