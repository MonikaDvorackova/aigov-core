apiVersion: v1
kind: Service
metadata:
  name: govai-core
  namespace: govai-core
  labels:
    app.kubernetes.io/name: govai-core
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: govai-core
  ports:
    - name: http
      port: 8088
      targetPort: http
      protocol: TCP
