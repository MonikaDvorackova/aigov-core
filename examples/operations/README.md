# Operations examples

Scalability, tiering, and compression guidance lives in `docs/operations/`. Use:

```bash
make scalability-patterns-check
```

For observability snapshots see `examples/observability/`.
