# Privacy examples

Privacy architecture and retention patterns are documented under `docs/privacy/`. This directory is reserved for future sample manifests (for example minimisation checklists).

Validation:

```bash
make privacy-architecture-check
```
