# Signature storage

CLA Assistant Lite writes `version1/cla.json` here on first successful Individual CLA signature.

Do not create `cla.json` manually — the action fails if the file is pre-created incorrectly.
